const search = require('./getData');
const assert = require("assert");

describe ('program', ()=>{
    describe('search', ()=>{
        it('returns the proper result for "MIA ZA\'s', () => {
            assert(true, true);
        });

    });

});